services:
  rusthole:
    build:
      context: .
    network_mode: host
    ports:
      - "53:53"
    
  reporting:
    build:
      context: .
      dockerfile: Dockerfile.reporting
    network_mode: host
